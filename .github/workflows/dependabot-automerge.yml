---
name: "Dependabot - Auto-merge"

# yamllint disable-line rule:truthy
on:
  pull_request_target:

permissions:
  pull-requests: write
  contents: write

jobs:
  dependabot-automerge:
    uses: timmo001/workflows/.github/workflows/dependabot-automerge-any.yml@1ed6e00bfeecec20f59b49bb96a40969ccc8821b # master
