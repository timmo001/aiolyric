---
name: "Sync labels"

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - master
    paths:
      - .github/labels.yml
  schedule:
    - cron: "34 5 * * *"
  workflow_dispatch:

jobs:
  labels:
    uses: timmo001/workflows/.github/workflows/labels.yml@1ed6e00bfeecec20f59b49bb96a40969ccc8821b # master
